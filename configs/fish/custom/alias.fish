# Enhanced commands
alias ls='eza -l --icons --git'
alias ll='eza -la --icons --git'
alias cat='bat --style=full'
alias lg='lazygit'

# Docker aliases with remote server support
alias dc='docker compose'
alias dps='docker ps'
alias dex='docker exec -it'
alias dl='docker logs -f'

# Create aliases for our custom functions
alias sc='ssh_connect'
alias dsel='docker_select'
